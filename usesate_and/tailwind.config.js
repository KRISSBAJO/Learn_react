/** @type {import('tailwindcss').Config} */
module.exports = {
  mode: 'jit', // Optional: Enable JIT mode
  purge: ['./src/**/*.{html,js,jsx}'], // Updated purge configuration
  theme: {
    extend: {},
  },
  plugins: [],
}
